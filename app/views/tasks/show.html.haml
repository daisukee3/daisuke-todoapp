.task_container
  %h1.task_title= @task.title
  .task_detail_actions
    .dropdown
      = image_tag 'Combined Shape.svg', class: 'dropbtn'
      .dropdown-content.mini
        = link_to 'Edit', edit_board_task_path(board_id: @task.board_id, id: @task.id)
        = link_to 'Delete',board_task_path(board_id: @task.board_id, id: @task.id), data: { method: 'delete' , confirm: 'Are you sure you want to delete it?' }
  .task
    - if @task.eyecatch.attached?
      .task_image
        = image_tag @task.eyecatch
    .task_content
      = @task.content

.task_comment.index
  .comment_label Comment
  - @comments.each do |comment|
    .task_comment
      %p= comment.content

= link_to new_board_task_comment_path(board_id: @task.board_id, task_id: @task.id) do
  .btn-secondary
    + Add new comment